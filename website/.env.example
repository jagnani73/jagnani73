# Spotify API (server-only)
SPOTIFY_CLIENT_ID!!
SPOTIFY_CLIENT_SECRET!
SPOTIFY_REFRESH_TOKEN!

# Neon Postgres — arcade analytics (server-only). Provisioned via the Vercel
# Neon Marketplace integration; pooled URL for runtime, unpooled for migrations.
DATABASE_URL!
DATABASE_URL_UNPOOLED!
