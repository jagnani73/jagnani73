# Claude Code MCP config — contains the Neon DB connection string (credentials)
.mcp.json

# env files (never commit secrets; the app's own ignore lives in website/.gitignore)
.env
.env.*
!.env.example

# Claude Code agent worktrees — transient checkouts, never part of the repo.
# One was committed as a gitlink once (648c8fa); this stops that recurring.
.claude/worktrees/
